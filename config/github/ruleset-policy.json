{
  "schema_version": 1,
  "status": "desired_policy",
  "scope": "default_branch",
  "target_branch": "main",
  "enforcement": "active",
  "rules": {
    "require_pull_request": true,
    "required_approving_reviews": 1,
    "dismiss_stale_reviews_on_push": true,
    "require_approval_of_most_recent_reviewable_push": true,
    "require_conversation_resolution": true,
    "require_status_checks": true,
    "require_branch_up_to_date": true,
    "block_force_pushes": true,
    "block_branch_deletion": true,
    "require_linear_history": true,
    "required_status_checks": [
      "AI Native Quality Gates / repository-integrity",
      "Dependency Review / dependency-review",
      "CodeQL / analyze-actions"
    ]
  },
  "merge_policy": {
    "preferred_method": "squash",
    "allow_merge_commit": false,
    "allow_rebase_merge": false,
    "allow_squash_merge": true,
    "allow_auto_merge": true,
    "allow_update_branch": true,
    "merge_queue": "recommended_when_available"
  },
  "review_policy": {
    "supervisor_cannot_self_approve_high_risk_changes": true,
    "security_critical_changes_require_independent_review": true,
    "shared_coordination_state_requires_supervisor_review": true
  },
  "notes": [
    "Apply through GitHub repository/organization rulesets when an authenticated administration-capable API is available.",
    "Do not claim this policy is enforced until repository ruleset state is verified.",
    "Required status-check names should be verified after their first successful runs before activating the ruleset."
  ]
}
