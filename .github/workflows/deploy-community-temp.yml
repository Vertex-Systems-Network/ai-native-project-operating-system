name: ANPOS Community E2E Smoke (temporary)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  e2e-smoke-disabled:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Temporary smoke workflow disabled after successful validation"
