# Durable PostgreSQL ledger
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require
# Optional direct/unpooled connection used preferentially by schema migrations.
DATABASE_URL_UNPOOLED=

# Public HTTPS origin of the deployed ANPOS commercial/community service.
# No path query or fragment; used to derive the fixed OAuth callback and Setup URL.
ANPOS_PUBLIC_BASE_URL=https://license.example.com

# Encrypts short-lived HttpOnly Community browser sessions and OAuth state.
# Generate a high-entropy random value (at least 32 characters) and store only in secrets management.
ANPOS_SESSION_SECRET=replace-with-at-least-32-random-characters

# Remote ephemeral execution sandbox gateway.
# The commercial service signs each exact request. The gateway must reject replayed nonce/timestamp
# combinations, run one isolated ephemeral workspace with network denied, and resolve only allowlisted
# environment variable NAMES server-side. Never put raw sandbox secrets in tool/model arguments.
ANPOS_SANDBOX_ENDPOINT=https://anpos-commercial-service.vercel.app/v1/execute
ANPOS_SANDBOX_DRIVER_ID=remote_ephemeral_signed_gateway_v1
ANPOS_SANDBOX_SIGNING_SECRET=replace-with-at-least-32-random-characters
ANPOS_SANDBOX_REQUEST_SKEW_SECONDS=120

# Dedicated public Repository Supervisor GitHub App.
# Keep this App distinct from the read-only Marketplace billing/Community App and the private Vendor Distribution App.
# Required repository permissions for the guarded write runtime should be the minimum needed for Metadata: read,
# Contents: write, Pull requests: write, and read-only checks/status visibility. Do not grant Administration by default.
ANPOS_GITHUB_SUPERVISOR_APP_ID=777777
ANPOS_GITHUB_SUPERVISOR_CLIENT_ID=Iv1.replace-with-supervisor-client-id
ANPOS_GITHUB_SUPERVISOR_CLIENT_SECRET=replace-with-real-supervisor-client-secret

# ChatGPT/Codex MCP OAuth client allowlists. Copy the exact HTTPS client metadata document URL
# and exact redirect URI shown by the plugin/MCP management surface for this connection.
# New ChatGPT connections commonly use callback-specific paths. Legacy published apps may retain
# the older stable client/redirect pair. Do not guess, shorten, or generalize either URL.
# The authorization server fetches the exact allowlisted CIMD document with redirects disabled
# and verifies its client_id, redirect_uris, authorization_code/code support, and public-client token method.
ANPOS_MCP_ALLOWED_CLIENT_IDS=https://chatgpt.com/oauth/REPLACE_CALLBACK_ID/client.json
ANPOS_MCP_ALLOWED_REDIRECT_URIS=https://chatgpt.com/connector/oauth/REPLACE_CALLBACK_ID
ANPOS_MCP_ACCESS_TOKEN_TTL_SECONDS=3600

# Internal Repository Supervisor read-only E2E grant.
# Never represents a Marketplace purchase and never grants write/planning capability.
# Keep the expiry within 48 hours and remove both variables after the live read E2E.
ANPOS_INTERNAL_SUPERVISOR_READ_TEST_LOGIN=
ANPOS_INTERNAL_SUPERVISOR_READ_TEST_EXPIRES_AT=

# Customer-facing/public GitHub Marketplace App.
# This App owns the Marketplace listing, Marketplace account reconciliation, and Community audit consent surface.
ANPOS_GITHUB_WEBHOOK_SECRET=replace-with-at-least-32-random-characters
ANPOS_MARKETPLACE_APP_ID=123456
ANPOS_MARKETPLACE_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"

# GitHub App OAuth client credentials for the Setup URL -> PKCE OAuth -> Community session flow.
# The client secret is server-side only. Never expose it to browser JavaScript or commit it.
ANPOS_MARKETPLACE_CLIENT_ID=Iv1.replace-with-real-client-id
ANPOS_MARKETPLACE_CLIENT_SECRET=replace-with-real-github-client-secret

# REAL GitHub Marketplace plan ID for the free Community offering after the listing exists.
# Keep Community outside ANPOS_MARKETPLACE_PLAN_MAP; it has zero paid entitlements.
ANPOS_COMMUNITY_MARKETPLACE_PLAN_ID=123455

# Separate vendor-only/private GitHub App.
# This App may be installed on the verified private template repository and, when higher paid
# tiers are intentionally activated, the distinct verified private premium repository.
# It must NOT reuse the Marketplace App ID/private key. Keep it private to the vendor account.
ANPOS_VENDOR_APP_ID=654321
ANPOS_VENDOR_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...different-key...\n-----END RSA PRIVATE KEY-----"

# Map REAL paid GitHub Marketplace plan IDs to paid ANPOS plan IDs after listing setup.
# Never add Community here; its identity is ANPOS_COMMUNITY_MARKETPLACE_PLAN_ID.
# If this map contains pro, team, or enterprise, premium distribution configuration below
# becomes mandatory for full /api/ready. A Developer-only map does not require premium config.
ANPOS_MARKETPLACE_PLAN_MAP='{"123456":"developer","123457":"pro","123458":"team","123459":"enterprise"}'

# Organization seat capacity is operator-controlled. Marketplace per-unit purchases
# use GitHub's unit_count when present; these values are the fail-closed capacity
# for flat-rate/null-unit plans. REPLACE these example business limits before launch.
ANPOS_ORG_SEAT_LIMITS='{"developer":1,"pro":1,"team":10,"enterprise":50}'

# Signed portable entitlement key (Ed25519 PKCS8 PEM required)
ANPOS_ENTITLEMENT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
ANPOS_ENTITLEMENT_KEY_ID=anpos-entitlement-2026-01
ANPOS_ENTITLEMENT_ISSUER=https://license.example.com
ANPOS_ENTITLEMENT_TTL_SECONDS=86400

# Operator-only reconciliation APIs
ANPOS_OPERATOR_TOKEN=replace-with-at-least-32-random-characters

# Vendor private template distribution. The VENDOR GitHub App installation must have
# Contents: read on this repo. Paid delivery is pinned to one exact immutable commit
# that has already been populated from a handoff-verified deterministic template export.
ANPOS_VENDOR_INSTALLATION_ID=12345678
ANPOS_PRIVATE_TEMPLATE_REPO=Vertex-Systems-Network/anpos-commercial-template
ANPOS_COMMERCIAL_RELEASE_REF=0123456789abcdef0123456789abcdef01234567

# Conditional Pro/Team/Enterprise premium distribution.
# Required by full /api/ready only when ANPOS_MARKETPLACE_PLAN_MAP activates pro, team,
# or enterprise. The premium repo MUST be distinct from ANPOS_PRIVATE_TEMPLATE_REPO and
# accessible to the same Vendor App installation with Contents: read.
# ANPOS_PREMIUM_RELEASE_REF is the exact immutable commit SHA that was verified offline with
# scripts/verify_premium_pack.py. Copy the successful verifier receipt's manifest_sha256 and
# content_set_sha256 here. Do not use a branch/tag or invent digests.
ANPOS_PRIVATE_PREMIUM_REPO=Vertex-Systems-Network/anpos-premium-pro
ANPOS_PREMIUM_RELEASE_REF=abcdef0123456789abcdef0123456789abcdef01
ANPOS_PREMIUM_MANIFEST_SHA256=aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
ANPOS_PREMIUM_CONTENT_SET_SHA256=bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb

# Optional collaborator-based provisioning is OFF by default because GitHub limits
# invitations per repository and this mode needs Administration: write permission
# on the VENDOR App only. Never add that permission to the Marketplace App.
ANPOS_COLLABORATOR_PROVISIONING_ENABLED=false

# Marketplace webhooks are small; bound request memory even if Content-Length is absent.
ANPOS_MAX_WEBHOOK_BYTES=1048576
